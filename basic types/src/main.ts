let myName = "Robin";
let meaningOfLife: number;
let isLoading: boolean;
let album: any;
let language: string | number | boolean;

myName = "John";
meaningOfLife = 45;
isLoading = true;
album = 45;
language = 34;

const sum = (a: number, b: number) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;
